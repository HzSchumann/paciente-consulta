spring:
  application:
    name: notificacao-service
  main:
    web-application-type: none
  graphql:
    schema:
      locations: classpath:graphql/notificacao/
  datasource:
    url: jdbc:h2:mem:notificacaodb
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
